<h3 class="text-center">List of Foods</h3>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @foods.each do |food| %>
      <%= render "food_row", food: food, cart_item: @cart_item %>
    <% end %>
  </div>
</div>

<%= paginate @foods %>
<%= page_entries_info @foods %>